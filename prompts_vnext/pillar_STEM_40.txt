Compute 17×23 and show the intermediate steps clearly.

Simplify: (3x^2 − 2x + 5) − (x^2 + 4x − 1).

Expand and simplify: (2x − 3)(x + 5).

Solve for x: 3(2x−5)=4x+7.

Solve the system: x+y=11, x−y=3. Provide x,y.

Convert 0.375 to a fraction in simplest form and explain.

If P(A)=0.6, P(B)=0.5, and P(A∩B)=0.3, compute P(A∪B).

A fair die is rolled twice. What is the probability the sum is 7? Show counting.

Simplify: (x^2−9)/(x−3) and state domain restrictions.

Differentiate: g(x)=x^3−4x^2+2x−1. Give g'(x).

Integrate: ∫(6x^2 − 8x + 2) dx. Provide the constant.

Solve a quadratic: x^2−5x+6=0 and factor it.

Given points (1,2) and (5,10), find slope and equation of the line.

Find gcd(84, 60) using Euclid’s algorithm steps.

A right triangle has legs 6 and 8. Find hypotenuse and area.

Convert (101101)_2 to decimal. Show place values.

Compute 1/3 + 1/6 + 1/12 in simplest form.

Solve: |2x−7|=5. Give all solutions.

A bag has 3 red and 2 blue balls. Draw 2 without replacement. Probability both red?

Compute the determinant of [[2,1],[5,3]].

Compute the expected value of a fair die roll and show formula.

A sequence is 2,5,8,11,... Find the 20th term AND the sum of the first 20 terms. Show reasoning.

A rectangle has perimeter 50 and length 15. Find width AND the diagonal length. Show steps.

If 2^x = 16, solve for x. Then solve 3^x = 81. Finally compare which grows faster near x=4 in one sentence.

Compute: 7! / (5!·2!) and interpret it as a binomial coefficient. Then compute C(7,2) directly to confirm.

Solve: 2x+3y=12, 4x−y=5. Provide exact fractions. Then verify by substitution.

Find the smallest n such that 1+2+...+n ≥ 100. Then compute the exact sum at that n. Show work.

Given vectors u=(1,2), v=(3,−1), compute u·v. Then determine if the angle is acute/obtuse and explain.

A function is strictly increasing on ℝ. Explain what that means. Then give a derivative-based sufficient condition and one counterexample.

Given a graph with 5 nodes in a line, how many edges? Then generalize to n nodes and give a formula.

Time complexity: for i in range(n): for j in range(i): print(i,j). State Big-O and justify in 2 sentences.

Prove: If n is odd, then n^2 is odd. Keep it short and rigorous.

Disprove the claim: “All prime numbers are odd.” Give the minimal counterexample and explanation.

Given an equation ax+b=0 with a≠0, solve for x and state the necessary assumption.

Explain why dividing by (x−3) in (x^2−9)/(x−3) requires a domain restriction.

Compute log2(32) and explain why.

Simplify: (a^3 b^−2)(a^−1 b^5) into a^? b^? and state exponent rules used.

Solve the recurrence: a1=3, a_{n+1}=a_n+2. Give closed form.

Given a list of numbers, describe an algorithm to find the median in O(n log n). Name the key step.

Given a number n, describe how to test if it is prime (simple method) and state its time complexity.
